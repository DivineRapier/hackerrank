package handshake

func handshake(n int) int {
	//Enter your code here. Read input from STDIN. Print output to STDOUT
	//var cases, n int

	//fmt.Scanf("%d", &cases)

	// for i := 0; i < cases; i++ {
	// 	fmt.Scanf("%d", &n)
	// 	fmt.Printf("%d\n", (n-1)*n/2)
	// }
	return (n - 1) * n / 2
}
